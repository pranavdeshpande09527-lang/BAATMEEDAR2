Build Backend Skeleton
Now build the backend, but only the structure first.

Core backend routes:
POST /checks
GET /checks/:id
GET /checks/:id/status
GET /checks/:id/result

Internal services:
InputService
ClaimExtractionService
ResearchPlannerService
SearchService
EvidenceReviewService
IndependentVerificationService
VerdictService
CacheService
ApiBudgetService
Do not connect all APIs at once. First make the backend move a check through fake/mock stages.

